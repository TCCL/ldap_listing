## ldap_listing.links.menu.yml

entity.ldap_listing_directory_section.collection:
  title: 'LDAP Directory Listing Sections'
  route_name: entity.ldap_listing_directory_section.collection
  description: 'Configure LDAP directory listing sections'
  parent: system.admin_structure

ldap_listing.settings:
  title: 'LDAP directory listing'
  route_name: ldap_listing.settings_page
  description: 'Configure settings for the LDAP directory listing module'
  parent: user.admin_index
