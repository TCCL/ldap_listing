{#
/**
 * @file
 * Default theme implementation for ldap_listing_directory_listing
 * render element.
 *
 * Available variables:
 * - manifest: List of all user info to embed in the page for front-end use
 * - sections: List of directory sections to render; each section contains the following object:
 *   - id: The section config entity ID
 *   - label: The section title text
 *   - abbrev: The abbreviated section title text
 *   - error: Flag indicating whether an error occurred and the section could not be loaded
 *   - header: List of lists containing header item values
 *   - body: List of user entries; each entry contains the following object:
 *     - dn: The user's distinguished name
 *     - emailLink: If non-empty, then contains a mailto link of the user's email address
 *     - userPageLink: If non-empty, then contains the link to the user's profile page
 *     - userImageLink: If non-empty, then contains the link to the user's profile image
 *     - rank: The rank classification of the user (Note: the list is already sorted by rank)
 *     - name: The user name
 *     - email: The user's raw email address
 *     - title: The user's job title
 *     - phone: The user's phone number
 *   - footer: List of lists containing footer item values
 *   - weight: The weight value assigned to this section
 * - preamble_message_lines: Array of lines to output containing the preamble message
 * - last_generated_message: Text representation of last generated date
 */
#}

<div class="ldap-listing-directory-listing">
  <div class="directory-listing-top-matter">
    <div class="directory-listing-search-box-wrapper">
      <input id="ldap-listing-directory-listing-search-box" class="directory-listing-search-box" placeholder="Search name/job/phone/department" type="search">
    </div>

    <script id="ldap-listing-directory-listing-manifest" type="application/json">{{ manifest|json_encode()|raw }}</script>

    <div id="ldap-listing-directory-listing-results-region" class="directory-listing-search-results"></div>

    {% if preamble_message_lines %}
      <div class="directory-listing-preamble-wrapper">
        {% for line in preamble_message_lines %}
          <p>{{ line }}</p>
        {% endfor %}
      </div>
    {% endif %}
  </div>

  <div class="directory-listing-sections">
    {% for section in sections %}
      <div class="directory-listing-section-wrapper">
        <div class="directory-listing-section">
          <div class="heading">
            <span>{{ section.label }}</span>
            {% if section.abbrev %}
              <span class="abbrev">{{ section.abbrev }}</span>
            {% endif %}
          </div>

          {% if section.header %}
            <div class="header header-footer">
              {% for entry in section.header %}
                <div class="entry">
                  {% for item in entry %}
                    <div class="entry-item">{{ item }}</div>
                  {% endfor %}
                </div>
              {% endfor %}
            </div>
          {% endif %}

          <div class="body">
            {% if section.error %}
              <div class="error">
                <div>An error occurred and this section of the directory could not be loaded.</div>
              </div>
            {% elseif section.body is empty %}
              <div class="empty">
                <span>No results</span>
              </div>
            {% endif %}

            {% for user in section.body %}
              <div class="user-entry">
                <div class="name user-entry-item">
                  {% if user.userPageLink %}
                    <a href="{{ user.userPageLink }}">{{ user.name }}</a>
                  {% else %}
                    <span>{{ user.name }}</span>
                  {% endif %}
                  {% if user.emailLink %}
                    <div class="mail-link-wrapper">
                      <a href="{{ user.emailLink }}" class="mail-link" title="{{ user.email }}">✉</a>
                    </div>
                  {% endif %}
                </div>
                <div class="title user-entry-item">{{ user.title }}</div>
                <div class="phone user-entry-item">{{ user.phone }}</div>
              </div>
            {% endfor %}
          </div>

          {% if section.footer %}
            <div class="footer header-footer">
              {% for entry in section.footer %}
                <div class="entry">
                  {% for item in entry %}
                    {% if not loop.last and entry[loop.index0+1] is null %}
                    <div class="entry-item nowrap">{{ item }}</div>
                    {% else %}
                    <div class="entry-item">{{ item }}</div>
                    {% endif %}
                  {% endfor %}
                </div>
              {% endfor %}
            </div>
          {% endif %}
        </div>
      </div>
    {% endfor %}
  </div>

  <div class="directory-listing-bottom-message">Last generated on {{ last_generated_message }}</div>
</div>
